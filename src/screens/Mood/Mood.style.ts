import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  heading: { fontFamily: 'Rubik', fontSize: 24, fontWeight: 'bold', textAlign: 'center' },
  moodsContainer: {
    marginTop: 32,
    marginBottom: 16,
    flexDirection: 'row',
    justifyContent: 'space-evenly',
  },
});
